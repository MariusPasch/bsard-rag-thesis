# Scripts — CLI entry points for the BSARD evaluation project.
#
# evaluate.py : reads a retrieval result JSON, computes all metrics, writes output
# compare.py  : compares two experiments with significance testing
